# 学习平台：任务手册（虚构示例）

## 创建任务

在任务页点击“新建任务”，填写标题和预计分钟数。预计时长必须在 5 到 480 分钟之间。创建后任务状态为待完成。

## 完成任务

在任务详情页点击“标记完成”。完成后任务仍可查看，状态显示为已完成。只有任务所有者可以修改任务状态。

## 导出任务

在任务页点击“导出”，可以下载 CSV 文件。CSV 包含任务编号、标题、预计分钟数和完成状态，不包含密码或登录信息。

